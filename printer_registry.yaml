version: v0.1.0
last_updated: [INSERT TIMESTAMP]
variant_templates:
  - Delta
  - Cartesian
  - CoreXY
promotion_policy:
  requires:
    - PHASE:VALIDATED
    - mutation_log entry
    - user approval
